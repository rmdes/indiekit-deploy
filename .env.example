# =============================================================================
# Indiekit Docker Compose — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Required variables are marked with [REQUIRED].
# Everything else is optional — leave blank to disable.

# =============================================================================
# [REQUIRED] Domain & Site
# =============================================================================

# Your domain name (used by Caddy for automatic HTTPS)
DOMAIN=example.com

# Site URL (must match DOMAIN, with https://)
SITE_URL=https://example.com

# Site name (shown in header, feeds, admin)
SITE_NAME=My IndieWeb Blog

# Site description (for meta tags and feeds)
SITE_DESCRIPTION=An IndieWeb blog powered by Indiekit

# Locale and timezone
SITE_LOCALE=en
SITE_TIMEZONE=UTC

# Categories (comma-separated, used in Micropub post creation)
SITE_CATEGORIES=blog,notes,links,photos

# =============================================================================
# [REQUIRED] Author Information
# =============================================================================

# Your name (shown in h-card, posts, feeds)
AUTHOR_NAME=Your Name

# Your bio
AUTHOR_BIO=Welcome to my IndieWeb blog.

# Avatar image path (relative to site, or absolute URL)
AUTHOR_AVATAR=/images/default-avatar.svg

# Optional author details
AUTHOR_TITLE=
AUTHOR_LOCATION=
AUTHOR_LOCALITY=
AUTHOR_COUNTRY=
AUTHOR_ORG=
AUTHOR_PRONOUN=
AUTHOR_EMAIL=
AUTHOR_KEY_URL=

# =============================================================================
# Social Links (for rel="me" verification and feed widgets)
# =============================================================================

# Individual handles (auto-generates social links)
GITHUB_USERNAME=
BLUESKY_HANDLE=
MASTODON_INSTANCE=
MASTODON_USER=
LINKEDIN_USERNAME=

# Or set all social links manually (overrides auto-generation)
# Format: "Name|URL|icon,Name|URL|icon"
# Example: "GitHub|https://github.com/user|github,Mastodon|https://mastodon.social/@user|mastodon"
SITE_SOCIAL=

# =============================================================================
# Syndication — Mastodon (optional)
# =============================================================================

# Get access token from your instance: Settings > Development > New Application
# MASTODON_INSTANCE and MASTODON_USER also used for social links above
MASTODON_ACCESS_TOKEN=

# =============================================================================
# Syndication — Bluesky (optional)
# =============================================================================

# Use an app password: Settings > App Passwords > Add App Password
# BLUESKY_HANDLE also used for social links above
BLUESKY_PASSWORD=

# =============================================================================
# Syndication — LinkedIn (optional)
# =============================================================================

# Option 1: Use the OAuth flow at /linkedin (recommended)
# Option 2: Set access token manually (expires after ~60 days)
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_AUTHOR_NAME=
LINKEDIN_PROFILE_URL=

# LinkedIn OAuth app credentials (from LinkedIn Developer Portal)
# Required for the /linkedin OAuth endpoint
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# =============================================================================
# Syndication — IndieNews (optional)
# =============================================================================

# Languages for IndieNews submission (comma-separated)
INDIENEWS_LANGUAGES=en

# =============================================================================
# Webmentions (optional)
# =============================================================================

# Get token from https://webmention.io/settings
WEBMENTION_IO_TOKEN=

# =============================================================================
# FULL PROFILE ONLY — GitHub Integration
# =============================================================================
# Requires: docker compose -f docker-compose.yml -f docker-compose.full.yml up

# Shows GitHub activity on /github page
GITHUB_TOKEN=
# Comma-separated repos to feature (e.g., "user/repo1,user/repo2")
GITHUB_FEATURED_REPOS=

# =============================================================================
# FULL PROFILE ONLY — Funkwhale Integration
# =============================================================================

# Shows listening activity on /funkwhale page
FUNKWHALE_INSTANCE=
FUNKWHALE_USERNAME=
FUNKWHALE_TOKEN=

# =============================================================================
# FULL PROFILE ONLY — Last.fm Integration
# =============================================================================

# Shows listening activity on /listening page
# Get API key from https://www.last.fm/api/account/create
LASTFM_API_KEY=
LASTFM_USERNAME=

# =============================================================================
# FULL PROFILE ONLY — YouTube Integration
# =============================================================================

# Shows YouTube activity on /youtube page
# Get API key from Google Cloud Console > APIs & Services > Credentials
YOUTUBE_API_KEY=
# Comma-separated channel handles (e.g., "@channel1,@channel2")
YOUTUBE_CHANNELS=

# =============================================================================
# FULL PROFILE ONLY — Podroll Integration
# =============================================================================

# Aggregates podcast episodes from FreshRSS
PODROLL_EPISODES_URL=
PODROLL_OPML_URL=

# =============================================================================
# System — Authentication
# =============================================================================

# SECRET is auto-generated on first run (JWT signing key).
# Only set manually if migrating from an existing deployment:
# SECRET=

# PASSWORD_SECRET — your admin password hash
#
# First-time setup:
#   1. Start the stack:  docker compose up -d
#   2. Visit https://your-domain/session/login
#   3. You'll see a "New password" page — create your password
#   4. Indiekit will display a PASSWORD_SECRET hash (starts with $2b$...)
#   5. Copy the hash and ESCAPE every $ as $$ before pasting below
#   6. Restart Indiekit:  docker compose restart indiekit
#
# IMPORTANT: Docker Compose interprets $ as variable substitution.
# You MUST double every $ sign in the hash:
#   Original:  $2b$10$abc123xyz...
#   Escaped:   $$2b$$10$$abc123xyz...
#
# Example: PASSWORD_SECRET=$$2b$$10$$abc123xyz...
PASSWORD_SECRET=
